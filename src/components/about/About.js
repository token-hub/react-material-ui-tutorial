import React from 'react'
import { Typography } from '@material-ui/core';

const About = () => {
	return (
		<Typography color='primary' variant='h4'> About page </Typography>
	)
}

export default About